// import { sequelize } from '../model';
// export const sq =sequelize;
export const { DB_HOST } = process.env;

export const { DB_PORT } = process.env;

export const { DB_DIALECT } = process.env;

export const { DB_NAME } = process.env;

export const { DB_USER_NAME } = process.env;

export const { DB_USER_PASSWORD } = process.env;
